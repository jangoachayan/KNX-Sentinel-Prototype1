# Changelog

## 1.2.0
-   **Documentation**: Massive overhaul of user documentation (`DOCS.md`).
-   **Fix**: Resolved "unknown error" build failures on Supervisor by defining compatible base images.
-   **Improvement**: Added `build-base` and `python3-dev` to Docker build process for better compatibility.
-   **Config**: Added `slug` and `url` to configuration.

## 1.1.1
-   Initial Prototype Release
-   Basic KNX monitoring and anomaly detection.
